# PLAN — AI 활용 국회 질의서 자동화 서비스

## 이번 사이클 목표
PRD.md에 정의된 두 가지 핵심 기능(질의서 자동 분류, 답변 초안 자동 생성)을 열린국회정보 API·OpenAI API와 연동해, 로컬에서 접수→분류→확정→초안생성→검토 흐름이 실제로 동작하는 Next.js 웹 앱으로 만든다.

## 성공 기준
- 질의서 1건을 입력하면 AI가 담당 실·국·과 후보 3개를 신뢰도 점수와 함께 제시한다.
- 후보 3개의 신뢰도가 모두 낮으면 "분류 보류 - 담당자 직접 확인 필요"로 표시된다.
- 담당자가 부서를 확정하면 500자 이내의 답변 초안이 자동 생성된다.
- 열린국회정보 API로 조회한 과거 질의·답변 데이터가 분류 근거·초안 참고자료로 화면에 함께 표시된다.
- 사실 확인이 필요한 수치나 개인정보로 보이는 내용에는 각각 "[확인 필요]" / "[개인정보 확인 필요]" 표시가 자동으로 붙는다.
- 로컬 개발 서버(localhost:3000)에서 전체 흐름이 오류 없이 끝까지 동작한다.

## 작업 순서

1. `.env`에 열린국회정보 API 키 항목을 추가하고 값이 채워져 있는지 확인한다.
2. 열린국회정보 API 연동 모듈(과거 질의·답변 데이터 조회 함수)을 만든다.
3. OpenAI API 연동 모듈(분류·초안 생성에 공통으로 쓸 호출 함수)을 만든다.
4. 질의서 텍스트를 입력하는 접수 화면을 만든다.
5. 질의서 자동 분류 API를 만든다 (신뢰도 상위 3개 후보 제시, 기준치 미달 시 보류 처리 포함).
6. 분류 결과를 보여주고 담당자가 부서를 확정하는 화면을 만든다.
7. 답변 초안 자동 생성 API를 만든다 (500자 제한, "[확인 필요]"·"[개인정보 확인 필요]" 표시, 검토 안내 문구 포함).
8. 생성된 초안을 보여주고 담당자가 검토·수정할 수 있는 화면을 만든다.
9. 예외 상황(텍스트 추출 실패, API 장애, 잘못 확정된 부서 재분류 등) 처리를 반영한다.
10. 담당자가 부서를 확정하고 초안을 수정한 이력을 남기는 기능을 추가한다.
11. 접수→분류→확정→초안생성→검토 전체 흐름을 로컬 환경에서 통합 테스트한다.
12. Vercel 배포를 준비하고 배포한다.
