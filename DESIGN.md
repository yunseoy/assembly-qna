# DESIGN — AI 활용 국회 질의서 자동화 서비스

PLAN.md에서 정한 작업 순서를 실제로 구현하기 위한 화면 구성, 데이터 흐름, 기술 선택을 정리한 문서입니다.

---

## 1. 화면 구성 (무엇이 어디에 보이는지)

1. **로그인 화면**
   - 담당자 계정으로 로그인하는 화면 (이번 실습에서는 단일 역할 계정 1개만 사용)

2. **질의서 접수 화면**
   - 국정감사 질의서 텍스트를 붙여넣는 입력창
   - "분류하기" 버튼

3. **분류 결과 화면**
   - 추천 담당 부서 후보 3개, 각각 신뢰도 점수와 함께 표시
   - 열린국회정보 API에서 찾은 과거 유사 질의·답변 사례를 후보 옆에 참고자료로 함께 표시
   - 후보 신뢰도가 모두 낮으면 "분류 보류 - 담당자 직접 확인 필요" 안내를 대신 표시
   - 담당자가 부서 하나를 골라 "이 부서로 확정" 버튼을 누르는 영역

4. **초안 결과 화면**
   - 확정된 담당 부서 정보
   - AI가 자동 생성한 답변 초안(500자 이내), "[확인 필요]" / "[개인정보 확인 필요]" 표시가 붙은 채로 표시
   - 담당자가 직접 고쳐 쓸 수 있는 수정 가능한 텍스트 영역
   - "저장" 버튼 (저장하면 처리 이력에 기록됨)

5. **처리 이력 화면**
   - 지금까지 처리한 질의서 목록과 각각의 담당 부서, 처리 일시를 표 형태로 표시

---

## 2. 데이터 흐름 (입력 → 처리 → 출력)

```
[담당자] 질의서 텍스트 입력
   ↓
[서버] 열린국회정보 API에서 과거 유사 질의·답변 사례 조회
   ↓
[서버] OpenAI API에 "질의서 텍스트 + 참고 사례" 전달 → 담당 부서 후보 3개 + 신뢰도 산출
   ↓
[화면] 후보 3개(또는 "분류 보류") 표시 → 담당자가 부서 확정
   ↓
[서버] 확정 정보 저장 + OpenAI API에 "확정 부서 + 참고 사례" 전달 → 답변 초안 생성
   ↓
[서버] 초안에 "[확인 필요]" / "[개인정보 확인 필요]" 표시 추가
   ↓
[화면] 초안 표시 → 담당자 검토·수정
   ↓
[서버] 저장 → 처리 이력에 기록
```

---

## 3. 기술 선택

### 그대로 유지 (STEP 2에서 만든 것)
- **Next.js** — 화면(프론트엔드)과 서버 쪽 코드(API)를 한 프로젝트 안에서 함께 만드는 도구. STEP 2에서 이미 뼈대를 만들어 뒀고, STEP 5에서 Vercel로 배포할 때 그대로 이어지므로 변경하지 않습니다.
- **Tailwind CSS** — 이미 프로젝트에 설치되어 있어, 화면의 색상·간격·버튼 모양 같은 디자인을 빠르게 입히는 데 그대로 사용합니다.

### 추가로 필요한 기술 (제안)
- **Supabase** — 질의서, 확정된 부서, 답변 초안, 처리 이력을 저장하고 담당자 로그인 기능까지 대신 처리해주는 서비스입니다.
  - 쉬운 설명: 데이터를 저장하는 창고와 문 앞 출입증 확인(로그인)을 직접 만들지 않고 빌려 쓰는 서비스예요.
- **openai (공식 라이브러리)** — 우리 서버가 OpenAI에 질문을 보내고 답을 받아오는 과정을 짧은 코드로 쉽게 해주는 공식 도구입니다.
  - 쉬운 설명: AI에게 말을 걸고 답을 받아오는 걸 대신 처리해주는 통역사 같은 도구예요.
- **pdf-parse** — 열린국회정보 API에서 받아온 과거 서면질의답변서(PDF) 파일을 열어서 안의 글자를 텍스트로 뽑아내는 도구입니다.
  - 쉬운 설명: PDF 파일을 열어서 안에 적힌 글자를 컴퓨터가 읽을 수 있는 텍스트로 바꿔주는 도구예요.
- **jszip** — HWPX 파일 내부(사실은 압축파일)를 열어서 그 안의 XML 텍스트를 꺼내는 도구입니다.
  - 쉬운 설명: HWPX는 사실 zip 압축파일이라서, 압축을 풀어야 안의 글자를 꺼낼 수 있어요. 그 압축을 푸는 도구예요.

### 과거 자료 형식 지원 범위 (이번 실습 기준)
- **PDF**: 지원함 — `pdf-parse`로 본문 텍스트를 추출해 분류·초안 생성 시 참고자료로 사용합니다.
- **HWPX**: 지원함 — HWPX는 zip 안에 XML이 들어있는 구조라서, `jszip`으로 압축을 풀고 텍스트 부분만 추출합니다. (열린국회정보의 서면질의답변서 목록은 실제로는 전부 PDF로 확인되어, 이 경로는 다른 HWPX 원문을 흉내낸 테스트 구조로 검증했습니다.)
- **HWP(옛 바이너리 형식)**: 향후 지원 예정 — 이번 실습에서는 다루지 않고, 화면에 "PDF/HWPX 외 형식 · 향후 지원 예정"으로만 표시합니다.
- **OCR(스캔본 이미지에서 글자 인식)**: 설계상의 향후 확장 지점으로만 남겨둡니다 — PDF/HWPX인데도 텍스트 추출이 실패하는 경우(스캔본 등)에 도입을 검토할 수 있으나, 이번 실습에서는 구현하지 않습니다.
