# CLAUDE.md

이 문서는 `my-app` 프로젝트에서 Claude Code(및 협업하는 개발자)가 따라야 할 규칙을 정의합니다.
현재 프로젝트는 초기 단계이며, 코드가 추가되는 대로 이 문서를 계속 업데이트해야 합니다.

## 프로젝트 개요

- **프로젝트명**: my-app
- **목적**: (작성 예정)
- **현재 상태**: 초기 세팅 단계 — 소스 코드 없음

## 핵심 작업 규칙

- 모든 설명과 주석은 한국어로 작성한다.
- 새 파일은 `my-app` 폴더 안에만 만든다. 폴더 바깥에는 파일을 생성하지 않는다.
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다.
- 파일을 삭제해야 할 때는 바로 지우지 않는다. `trash-can` 폴더를 만들어 그 안으로 옮겨두고, 실제 삭제는 사용자가 직접 확인 후 진행한다.
- 이미 설치된 서브에이전트(subagent)는 필요할 때마다 적극적으로 활용한다.

## 기술 스택

- **프레임워크**: Next.js로 고정 (PRD에서 확정). 다른 프레임워크로 변경하거나 마이그레이션을 제안하지 않는다.
- **배포**: Vercel 사용. 배포 작업이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증한다.
- **데이터베이스**: Supabase 사용 가능성 있음. 필요 시 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
- AI/LLM: OpenAI API 사용 가능성 (`.env`에 `OPENAI_API_KEY` 존재)
- 저장소/CI: GitHub 사용 가능성 (`.env`에 `GITHUB_TOKEN` 존재)

## 환경 변수

- 모든 비밀 값은 `.env`에 보관하며, 절대 코드나 커밋에 하드코딩하지 않습니다.
- `.env`는 `.gitignore`에 등록되어 git에 올라가지 않습니다.
- 새로운 비밀 값이 필요하면 `.env`에 키만 추가하고, 값은 사용자가 직접 채웁니다.
- 필요한 키 목록: `GITHUB_TOKEN`, `SUPABASE_ACCESS_TOKEN`, `VERCEL_TOKEN`, `OPENAI_API_KEY`

## 코딩 컨벤션

(스택 확정 후 작성 예정 — 들여쓰기, 네이밍 규칙, 폴더 구조 등)

## 커밋 / PR 규칙

(작성 예정 — 커밋 메시지 형식, 브랜치 전략 등)

## 테스트 / 빌드 명령어

(작성 예정 — `npm run dev`, `npm test` 등 실제 명령어가 정해지면 기록)

## 작업 방식 (검증 루프)

모든 구현 작업은 아래 루프를 매번 반복한다.

1. **구현한다** — 요청받은 변경 사항을 코드에 반영한다.
2. **결과를 직접 확인한다** — 실행하거나 열어보는 등 실제로 동작을 확인한다. (예: 서버 실행, 브라우저로 열기, 테스트 실행)
3. **스스로 코드 리뷰한다** — 작성한 코드를 다시 훑어보며 문제, 불필요한 복잡성, 규칙 위반 여부를 점검한다.
4. **문제가 있으면 고치고 1)로 돌아간다** — 통과할 때까지 루프를 반복한다.
5. **통과하면 보고한다** — 무엇을 왜 바꿨는지 한 줄로 보고한다.

## 보안 주의사항

- 비밀 값(API 키, 토큰 등)을 화면에 출력하거나 로그에 남기지 않습니다.
- `.env` 등 비밀 정보 파일은 `.gitignore`에 등록해 두고, 절대 커밋하지 않습니다.
- 외부 서비스 인증이 필요할 때는 토큰 값을 사용자에게 묻거나 채팅에 출력하지 않고, `.env`에 있는 값을 읽어서 사용합니다.
  - 예: Supabase 작업 → Supabase CLI 설치 후 `.env`의 `SUPABASE_ACCESS_TOKEN` 사용
  - 예: Vercel 작업(배포 등) → Vercel CLI 설치 후 `.env`의 `VERCEL_TOKEN`으로 인증

## 변경 이력

- 2026-07-28: 초기 초안 작성 (빈 프로젝트 상태)
- 2026-07-28: 핵심 작업 규칙 추가 (한국어 작성, 파일 위치 제한, 변경 사항 한 줄 보고, trash-can 방식 삭제, 서브에이전트 활용, 기술 스택 고정, 토큰 사용 방식)
- 2026-07-28: "작업 방식(검증 루프)" 섹션 추가 (구현→확인→셀프 리뷰→수정 반복→보고)
